import type { SystemStyleObjectRecord } from '@chakra-ui/react';

const styles: SystemStyleObjectRecord = {
  global: {
    body: {
      minWidth: '100%',
    },
    html: {
      minWidth: '100%',
    },
  },
};

export default styles;
